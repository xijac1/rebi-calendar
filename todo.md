i need to add order number to tasks - so user can put order or select none

the rebalance feauture should not put cards out of order on the calendar unless the card has no order

fix user_settings table to actually save user settings maybe try making a new account? 

